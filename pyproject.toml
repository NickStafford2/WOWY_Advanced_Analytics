[project]
name = "wowy"
version = "0.0.1"
description = "Simple game-level WOWY basketball impact prototype"
readme = "README.md"
requires-python = ">=3.12,<3.13"
authors = [{ name = "Nicholas Stafford" }]
dependencies = ["nba_api>=1.7,<2.0"]

[project.scripts]
wowy = "wowy.main:main"
wowy-ingest-nba = "wowy.ingest_nba_cli:main"
wowy-combine-games = "wowy.combine_games_cli:main"

[dependency-groups]
test = ["pytest>=8.0,<9.0"]

[build-system]
requires = ["poetry-core>=1.8.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
packages = [{ include = "wowy", from = "src" }]
